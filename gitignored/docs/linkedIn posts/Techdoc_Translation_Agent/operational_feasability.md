What about the operational feasibility of TechDoc Translation Agent? Can costs run away fast since it uses LLMs?

The number of LLM-calls made have been optimised to be as low as possible yet return as much value as possible for those calls. 

Therefore, it consumes the same magnitude of requests/tokens as any tech-savvy human translator would consume using an LLM as an AI assistant in the daily workflow.